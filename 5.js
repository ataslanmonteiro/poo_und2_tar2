const prompt = require("prompt-sync")();
let num = parseInt(promt("Digite um número: "));
let primo = true;
if (num <= 1) {
  primo;
}
for (let i = num1; i <= num2; i++) {
  if (i % 2 !== 0) {
    console.log(i);
  }
}
